[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["backend"]
addopts = "-p no:cacheprovider"
markers = ["real_auth: exercise the real authentication dependency instead of the legacy test override"]
